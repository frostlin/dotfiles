
# from wiki for nvidia compatibility
env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
env = XDG_CURRENT_DESKTOP,sway
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = NVD_BACKEND,direct
env = ELECTRON_OZONE_PLATFORM_HINT,auto
env = WLR_NO_HARDWARE_CURSORS,1



cursor {
    no_hardware_cursors = true
}

# monitors
monitor=DP-1,preferred,0x0,1
monitor=HDMI-A-1,preferred,1800x-1080,2
#monitor=HDMI-A-1,preferred,1x1440,1

# Set programs that you use
$terminal = kitty
#$terminal = tilix -a app-new-window
$fileManager = ranger
$menu = sh ~/.config/rofi/bin/launcher
#$menu = wofi -s ~/.config/wofi/menu.css


### AUTOSTART ###
# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
exec-once = waybar 
exec-once = firefox 
exec-once = telegram-desktop

exec-once = hyprctl dispatch workspace 1
exec = xrandr --output DP-1 --primary 
exec = hyprpaper
exec = hyprctl keyword decoration:screen_shader /home/paul/.config/hypr/shaders/blue_light_shader_v2.frag

### ENVIRONMENT VARIABLES ###
# See https://wiki.hyprland.org/Configuring/Environment-variables/
env = XCURSOR_SIZE,30
env = HYPRCURSOR_SIZE,30
env = TERM,$terminal
env = QT_QPA_PLATFOMTHEME,qt6ct


### LOOK AND FEEL ###
# Refer to https://wiki.hyprland.org/Configuring/Variables/
# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 5
    gaps_out = 10
    border_size = 0

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(cccccccc)
    col.inactive_border = rgba(55555555)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle

}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 15

    #screen_shader = grayscale.glsl

    # Change transparency of focused and unfocused windows
    active_opacity = 1
    inactive_opacity = 0.85

    #drop_shadow = true
    #shadow_range = 4
    #shadow_render_power	= 4
    #col.shadow = 0xff0f0f0f
    rounding = 8
    
    #layerrule = blur,waybar



    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 5
        passes = 4
        ignore_opacity = true
        
        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
  #  enabled = true

  #  # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0, 1, 0, 1

    animation = windows, 1, 5, myBezier, popin 80%
    animation = windowsOut, 1, 3, default, popin 90%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 4, default
    animation = workspaces, 1, 4, default
    animation = specialWorkspace, 1, 7, default, slidevert


  #enabled = yes

  #bezier = wind, 0.05, 0.9, 0.1, 1.05
  #bezier = winIn, 0.1, 1.1, 0.1, 1.1
  #bezier = winOut, 0.3, -0.3, 0, 1
  #bezier = liner, 1, 1, 1, 1
  #bezier = overshot, 0.05, 0.9, 0.1, 1.05
  #bezier = smoothOut, 0.5, 0, 0.99, 0.99
  #bezier = smoothIn, 0.5, -0.5, 0.68, 1.5
  #
  #animation = windows, 1, 6, wind, popin
  #animation = windowsIn, 1, 3, winIn
  #animation = windowsOut, 1, 2, winIn
  #animation = windowsMove, 1, 5, wind
  #animation = border, 1, 1, liner
  #animation = fade, 1, 3, smoothOut
  #animation = workspaces, 1, 5, overshot
  #animation = borderangle, 1, 180, liner, loop #used by rainbow borders and rotating colors
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}

render {
    explicit_sync = 1
    explicit_sync_kms = 0
}

### INPUT ###
# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us,ru
    kb_variant =
    kb_model =
    kb_options = grp:win_space_toggle
    kb_rules =

    repeat_delay = 250
    repeat_rate = 50
    special_fallthrough = true

    follow_mouse = 2

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = true
    workspace_swipe = 1
    workspace_swipe_fingers=3
    workspace_swipe_distance=400
    workspace_swipe_min_speed_to_force=30
    workspace_swipe_create_new = true
    workspace_swipe_cancel_ratio = 0.5
    workspace_swipe_forever = true

}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}
binds {
  workspace_center_on = 1
}




##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

workspace = 1, monitor:DP-1, persistent:true
workspace = 2, monitor:DP-1, persistent:true
workspace = 3, monitor:DP-1, persistent:true
workspace = 4, monitor:DP-1, persistent:true
workspace = 5, monitor:DP-1, persistent:true
workspace = 6, monitor:DP-1, persistent:true
workspace = 7, monitor:DP-1, persistent:true
workspace = 8, monitor:DP-1, persistent:true
workspace = 9, monitor:DP-1, persistent:true
workspace = 10, monitor:DP-1, persistent:true
workspace = 11, monitor:HDMI-A-1, persistent:true, gapsout:0, gapsin:1,rounding:false
workspace = 12, monitor:HDMI-A-1, persistent:true, gapsout:0, gapsin:1,rounding:false
#workspace = s[true], gapsin:20, gapsout:20

windowrulev2 = suppressevent maximize, class:.* 
windowrulev2 = float, size 20% 20%, title:^(floating)$
windowrulev2 = fullscreen, title:^(fullscreen)$

#windowrulev2 = opacity 1, class:^(firefox)$
windowrulev2 = float, class:^(mpv)$
#windowrulev2 = float, class:^([Ss]team)$, title:^((?![Ss]team).*|[Ss]team [Ss]ettings|Friends List)$
#windowrulev2 = noanim, class:^(ueberzug.*)$


#windowrulev2 = workspace 1, class:^([Ff]irefox)$
windowrulev2 = float, class:^(.*[Tt]elegram.*)$
windowrulev2 = workspace 2, class:^(.*[Dd]iscord.*|[Vv]esktop)$
#windowrulev2 = workspace 10, class:^([Ss]team)$
windowrulev2 = stayfocused, title:^()$,class:^(steam)$
windowrulev2 = minsize 1 1, title:^()$,class:^(steam)$








# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Return, exec, $terminal
bind = $mainMod SHIFT, Return, exec, kitty --title=fullscreen
bind = $mainMod CTRL, Return, exec, kitty --title=floating
bind = $mainMod ALT, Return, exec, tilix -a app-new-window
#bind = $mainMod SHIFT, Return, exec, tilix -a app-new-window --title=fullscreen
#bind = $mainMod CTRL, Return, exec, tilix -a app-new-window --title=floating
bind = $mainMod, C, killactive,
bind = $mainMod SHIFT, C, closewindow,
bind = $mainMod SHIFT, Q, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, R, exec, killall waybar;waybar &
bind = $mainMod SHIFT, R, exec, hyprctl reload
bind = $mainMod, F, togglefloating,
bind = $mainMod, M, fullscreen,
bind = $mainMod, P, exec, pulseaudio-control --node-blacklist "alsa_output.pci-0000_01_00.1.hdmi-stereo-extra2" next-node,
bind = $mainMod, escape, exec, hyprlock &,

bind = $mainMod, D, exec, $menu
bind = $mainMod, S, exec, ~/.config/rofi/bin/ssh_menu.sh
#bind = SUPER, SUPER_L, exec, killall wofi
#bindr = SUPER, SUPER_L, exec, wofi -s ~/.config/wofi/menu.css

#bind = $mainMod, P, pseudo, # dwindle
#bind = $mainMod, J, togglesplit, # dwindle
bind = , Print, exec, hyprctl keyword decoration:screen_shader ''; hyprshot -m region -zsr -- | swappy -f -; hyprctl keyword decoration:screen_shader /home/paul/.config/hypr/shaders/blue_light_shader_v2.frag


bindel = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindl = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindl = , XF86AudioPlay, exec, playerctl -p spotify,firefox,telegram-desktop play-pause
bindl = , XF86AudioPrev, exec, playerctl -p spotify,firefox,telegram-desktop previous
bindl = , XF86AudioNext, exec, playerctl -p spotify,firefox,telegram-desktop next

# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10

# sets repeatable binds for resizing the active window
binde = $mainMod ALT,L,resizeactive,10 0
binde = $mainMod ALT,H,resizeactive,-10 0
binde = $mainMod ALT,K,resizeactive,0 -10
binde = $mainMod ALT,J,resizeactive,0 10

# Example special workspace (scratchpad)
bind = ,F12, togglespecialworkspace, terminal
bind = CTRL, F12, movetoworkspace, special:terminal
bind = SHIFT,F11, togglespecialworkspace, magic2
bind = CTRL, F11, movetoworkspace, special:magic2
bind = SHIFT,F10, togglespecialworkspace, magic3
bind = CTRL, F10, movetoworkspace, special:magic3

bind = $mainMod, I, exec, pkill -SIGUSR1 waybar

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
bindm = $mainMod, mouse:274, killactive

